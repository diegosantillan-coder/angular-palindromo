import { Routes } from '@angular/router';
import { PalindromeCheckerComponent } from './palindrome-checker.component';

export const routes: Routes = [
	{ path: '', component: PalindromeCheckerComponent },
];
